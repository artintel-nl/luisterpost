# internal planning docs — never publish
PUBLISHING-PLAN.md
DATA-PIPELINE.md
STRUCTURE-PLAN.md

# The publisher is a good candidate to publish eventually — it's the most interesting
# part of the architecture and proves exactly what does and doesn't leave the house.
# Held back until Frank reads it: the comments name household appliances on the broker
# (washing machine, dryer, IronMan, desk lamp, alarm) and the private topic layout.
publisher/
.resume_task.md
.claude/

# os / editor noise
.DS_Store
__pycache__/
*.pyc
# keep the raw handoff private; brand/ is the public copy
design_handoff_luisterpost_logo/

# Node firmware. Held back with publisher/ until we decide what goes public —
# organisation now, publication decision later.
nodes/

# Credentials never go in, even if the folders above are later un-ignored.
# The ** form survives that change; a path under an ignored dir would not.
**/secrets.h
